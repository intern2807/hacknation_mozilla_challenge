# Harbor Build Credentials
# =============================================================================
# These credentials are used during the build/release process.
# They are NOT shipped with the app.
#
# Copy to: config/build.env
# Usage:   Loaded by build scripts during release
# =============================================================================

# -----------------------------------------------------------------------------
# Firefox Extension ID (REQUIRED for extension signing)
# -----------------------------------------------------------------------------
# Must be unique per Mozilla Add-ons account.
# Format: name@domain.com (letters, numbers, dots, hyphens, underscores only)
#
# This ID is registered with Mozilla when you first sign the extension.
# Use a different ID for each developer account.

EXTENSION_ID="your.name.harbor@gmail.com"

# -----------------------------------------------------------------------------
# Mozilla Add-ons API (REQUIRED for extension signing)
# -----------------------------------------------------------------------------
# Get these from: https://addons.mozilla.org/developers/addon/api/key/
#
# Steps:
# 1. Sign in to addons.mozilla.org
# 2. Go to Tools > Manage API Keys
# 3. Generate new credentials

AMO_JWT_ISSUER="user:12345678:123"
AMO_JWT_SECRET="your-64-character-hex-secret-here"

# -----------------------------------------------------------------------------
# Apple Developer (OPTIONAL - for macOS pkg signing and notarization)
# -----------------------------------------------------------------------------
# Required for distributing without Gatekeeper warnings.
# Requires Apple Developer Program membership ($99/year).
#
# If not set, build will skip signing gracefully.

# Your Developer ID Installer certificate name
# Find with: security find-identity -v | grep "Developer ID Installer"
# DEVELOPER_ID="Developer ID Installer: Your Name (XXXXXXXXXX)"

# Your Apple ID email (for notarization)
# APPLE_ID="your@email.com"

# Your Apple Developer Team ID
# APPLE_TEAM_ID="XXXXXXXXXX"

# Note: Also run this once to store notarization password in keychain:
#   xcrun notarytool store-credentials "AC_PASSWORD" \
#     --apple-id "your@email.com" \
#     --team-id "XXXXXXXXXX" \
#     --password "xxxx-xxxx-xxxx-xxxx"

