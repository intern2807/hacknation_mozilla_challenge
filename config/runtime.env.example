# Harbor Runtime Configuration
# =============================================================================
# Optional user overrides for Harbor settings.
#
# Location: ~/.harbor/config.env
# Usage:    Loaded at runtime, overrides built-in defaults
# =============================================================================

# -----------------------------------------------------------------------------
# OAuth Provider Overrides
# -----------------------------------------------------------------------------
# Use your own OAuth credentials instead of Harbor's built-in ones.
# Useful for enterprise deployments or if you want full control.

# HARBOR_GOOGLE_CLIENT_ID=""
# HARBOR_GOOGLE_CLIENT_SECRET=""

# HARBOR_GITHUB_CLIENT_ID=""
# HARBOR_GITHUB_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# LLM Settings
# -----------------------------------------------------------------------------
# Default LLM provider and model

# HARBOR_DEFAULT_LLM_PROVIDER="ollama"
# HARBOR_DEFAULT_LLM_MODEL="llama3.2"

# Ollama host (if not localhost)
# OLLAMA_HOST="http://localhost:11434"

# -----------------------------------------------------------------------------
# Storage Paths
# -----------------------------------------------------------------------------
# Override default storage locations

# HARBOR_DATA_DIR="~/.harbor"
# HARBOR_SECRETS_DIR="~/.harbor/secrets"
# HARBOR_SESSIONS_DIR="~/.harbor/sessions"

# -----------------------------------------------------------------------------
# Development / Debug
# -----------------------------------------------------------------------------
# Enable debug logging

# HARBOR_DEBUG="true"
# HARBOR_LOG_LEVEL="debug"

